# 2.0 verification record

This file preserves historical results from the original rewrite worktree. Current standalone-repository checks and pending correction retests are in [checkpoint.md](checkpoint.md). The code census below predates the latest synergy corrections and is not an exact count of this checkpoint.

Candidate: uncommitted `codex/lords-ledger-v2` worktree based on `47570f9caa5696c7369d88a42ae87171b78cd68b`. The original `main` checkout remains clean at that commit. Results below are tied to the code state when run; the companion behavioral checkpoint was independently accepted at `3C4BB41BD5F9A343DB9512BD0354137F85DFA29C75BAFF02C19E89972C1B410C`. The shared-view conversion and mobile retests were independently accepted at `C66BA1FFF5426BCAAD132966AE54B7AB430A3B758108951DC144325DC7B1827B` in the inspected paths.

## Baseline and broad suite

Baseline `npm ci`, `npm run build`, and `npm run lint` passed; `npx playwright test --list` discovered 109 tests in 16 files. A reliable full baseline browser suite was not run because the old config could reuse an unrelated server on port 5173. The candidate `playwright.config.ts` starts its own strict server on 127.0.0.1:5182 with no reuse.

An earlier candidate full `npx playwright test --workers=1 --reporter=line` run discovered 117 tests: 103 passed, 13 failed, 1 skipped, in 20.6 minutes. All 13 failures were missing `chromium-win32` screenshot goldens; tracked goldens are `chromium-linux`. Generated Windows images are retained only in ignored `artifacts/v2/full-suite-windows-golden-candidates/` pending visual review. This full suite predates the latest save, selector, and navigation changes and has not been rerun at the current fingerprint. The six legacy helper profiles from that run are in the earlier record; they were unseeded and not then asserted.

## Current focused checks and campaigns

- `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check`: passed after the Feast resolver and UI changes, before this documentation update. ESLint excludes generated `artifacts` and `test-results`; the archived baseline source briefly caused nine lint errors until that generated path was excluded, then lint was rerun. `strict` and `noUncheckedIndexedAccess` apply to migrated TypeScript. `allowJs: true` and `checkJs: false` still leave most application and tests unchecked. An earlier non-gating `npx tsc --project tsconfig.json --checkJs true --pretty false` probe reported 2,215 diagnostics in `artifacts/v2/checkjs-probe-final.txt`; it predates this slice. A mistaken `npx vitest run tests/unit` invocation discovered Node test files but reported ten empty suites because this repository uses Node's built-in runner; it is not counted as a unit result.
- `npm run test:unit`: 62 discovered, 62 passed, including saved random replay, terminal save reasons, canonical Watchtower scoring, target spacing at 320px, duplicate-command rejection, raid-result save, four Gambit resolver/action regressions, five cellar spawn/scoring/save regressions, three seeded stranger offer/forgery/save regressions, four Bard content/reward/save regressions, six companion offer/content/save/raid regressions, and earlier transaction cases. The focused save/building/forge browser run passed 13; `navigation-v2.spec.ts` passed 1; `game-start.spec.js` passed 8; `raid-v2.spec.ts` passed 3 fixture-assisted warning→result→continue flows on isolated port 5182. `watchtower-v2.spec.ts` passed 2. `gambit-v2.spec.ts` passed 4. `rats-v2.spec.ts` passed one real 20-second minigame. `stranger-v2.spec.ts` passed 2. `bard-v2.spec.ts` passed one seeded riddle/save/reentry path. `companions-v2.spec.ts` passed 10 real UI paths: pending Marta storage offer across load and canonical purchase, plus Aldric's free lesson on mobile. The frozen combined 18 Bard/companions/Gambit/cellar/stranger tests passed in 1.3 minutes after the view and mobile note correction. An earlier eight-test combined run passed seven and timed out clicking the tutorial during the Stranger trade flow while build/document writes ran concurrently; a separate two-test Stranger run and a frozen combined rerun then passed. The timeout's cause is not established; it is preserved as a first-attempt failure. The companion browser tests initially failed their 44px touch-target assertion because both offer actions measured 38px; the controls now measure at least 44px and the same tests pass. The earlier `initial-state-preflight/compare.mjs` intentionally no longer passes because seeded prices and `rngState` differ. Its successor, `node artifacts/v2/seeded-startup-parity/compare.mjs`, passes for Easy/Normal/Hard starts and Hard replay after excluding only those two seeded fields; see its `result.json`.
- The latest `npm run test:unit` discovered 67 and passed all 67, adding five Feast command/save cases. The missing-history regression first failed with the reviewers' exact `TypeError`, then passed after the reducer correction. `npx playwright test tests/e2e/gameplay/feast-v2.spec.ts tests/e2e/gameplay/save-v2.spec.ts --workers=1` passed 13/13, including a real 390px continuation from a validator-accepted older Hall shape. The Feast browser cases check selected-option `aria-pressed`, seeded preview, a pending save/reload, canonical settlement, once-per-season lockout, exact RNG/meters/history, and mobile viewport positions. The previous 13/13 Feast/navigation/save run was before the reviewer fixes; navigation was not rerun afterward.
- After the Market slice, `npm run test:unit` discovered 72 and passed all 72. `npx playwright test tests/e2e/gameplay/market-haggle-v2.spec.ts tests/e2e/gameplay/save-v2.spec.ts tests/e2e/gameplay/feast-v2.spec.ts --workers=1` passed 14/14 after the market price/save checks. One market browser path drove a real 390px five-grain sale through pending save, reload, the displayed offer, settlement, resource assertions, and once-only clearing. It also captured a 1366px pending view. Earlier variants failed a mistaken test expectation of one rather than the UI's default five units and then reproduced the hidden mobile merchant heading before each scroll correction; the final test passes for the actual UI behavior.
- The first independent Market review at `2513A333…` found six actionable issues: a pending Edmund deal under Giovanni, missing foreign premium sell routes, 22/111 season-expanded authored buy offers without prices, an understock sale overstating earnings, malformed accepted-status reputation producing NaN, and a foreign card/stall tier mismatch. Primary fixes use a shared typed quote function, merchant-specific quick trade payloads, full-quantity settlement, an understock explanation, pending-stall lock, and consistent reputation validation/display. Three red unit cases now pass, plus a merchant quote/debit unit case. Current `npm run test:unit`: 76/76; `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check`: pass. `npx playwright test tests/e2e/gameplay/market-haggle-v2.spec.ts tests/e2e/gameplay/save-v2.spec.ts tests/e2e/gameplay/feast-v2.spec.ts --project=gameplay --workers=1 --retries=0`: 16/16. `node --experimental-strip-types artifacts/v2/seeded-campaigns/simulate.mjs`: 100 campaigns, 55 victories, 45 losses, turns 11–40, zero save failures. Independent retest of this repair is pending. A client-owned save can still fabricate a plausible accepted negotiation by editing multiple fields; the current validator establishes bounded terms and consistency, not authenticity.
- Tester and grader independently retested frozen `F9EC9720…`: all original normal-path and graded P2/P3 findings passed their exact reproductions, including 111/111 authored season-expanded offers, all six foreign premium sales, complete understock refusal, rejected corrupt accepted-status save, and matching foreign relationship labels. Tester confirmed T-MKT03 P3 remains: a plausible manually edited accepted save skips the seeded negotiation; client-owned saves do not provide cryptographic authenticity. Both independently found a new no-pending corrupt-reputation route (grader G-MKT-R01 P2, tester T-MKT-R01 P3). A red unit case reproduced save acceptance of `'bad'`/`null` reputation with `activeHaggle:null`; root market reputation validation and Walk Away/Start guards now reject it. Local checks after the fix: 77/77 units, 4/4 Market browser paths including visible 390px save rejection without overwriting bytes, 14/14 Market/save paths before adding that browser case, typecheck/lint/build, original grader direct probe rejects import, and 100 seeded campaigns 55/45 with zero save failures. Independent retest of G-MKT-R01 remains pending.
- Both reviewers independently accepted the reputation fix at frozen `404DF7AE…`: no-pending malformed save rejects, direct malformed pending Walk Away/Start do not mutate, legitimate Walk Away still changes 50→48, earlier v2 and legacy saves still load. Tester additionally verified the 390px visible error, unchanged save bytes, zero browser errors, and ordinary Edmund/Giovanni/Bjorn bargains. Reports: `artifacts/v2/{tester,grader}-market-reputation-retest/`. An adjacent inconsistency then surfaced in code inspection: earned trade/wool synergies raised actual quick-sale proceeds but the Market showed the lower base quote. The reducer and merchant/global Quick Trade now use one typed bonus-inclusive quote; haggle setup explicitly shows its base reference. The bonus regression checks base +3d against a real one-wool sale in both UIs at 390px. Latest local checks: `npm run test:unit` 78/78, `npm run typecheck`, `npm run lint`, `npm run build` pass; 18/18 focused Market/Feast/save browsers; 100 seeded campaigns 55 victories/45 losses/zero save failures. Independent review of the bonus display is pending.
- At bonus-review fingerprint `910D543E…`, the independent tester earned `market_king_1` and `wool_baron_2` through legal actions and observed Agnes and Quick Trade quote/credit 9d Wool at 390/1366px, and Fatima premium quote/credit 11d at 390px. The grader independently calculated ordinary, wool, and two foreign premium combinations; all valid quotes and payouts matched. Both then found P2 T-MKB01/G-MBON-01: a validator-accepted malformed saved `synergies.activated` string makes a sale silently no-op while showing 0d or base price. The grader also measured P3 G-MBON-02: 12px sale prices at 4.28:1 contrast. Red unit reproduced the invalid save; save import now requires a unique list of authored synergy IDs, and a 390px browser test rejects the damaged save without replacing its bytes. Small sale prices now use `#64ad50` on `#1a1610`, calculated 6.55:1. Both reviewers independently accepted the exact P2/P3 retest at `F16387D5…`; reports are in `artifacts/v2/{tester,grader}-market-bonus-retest/`. The grader identified a further P3 direct-state invented/duplicate synergy path. A new unit reproduced the sale before the shared validator also guarded direct quick sales. The latest 80/80 unit result covers this guard, which awaits independent retest.
- The unused `TradeTab.jsx` had no production import; the original App already rendered MarketSquare. Its three salt/tools/spices purchase hints were copied into the active Quick Trade view before deletion, and Buy/Sell 1/5/All remain. The Market browser assertion checks these hints, 44px Quick Trade targets, and no horizontal overflow at 390 and 320px. An initial focused run caught a 36px mobile CSS override; the dedicated button rule made the rerun pass 6/6. Current combined `npx playwright test tests/e2e/gameplay/market-haggle-v2.spec.ts tests/e2e/gameplay/save-v2.spec.ts tests/e2e/gameplay/feast-v2.spec.ts --project=gameplay --workers=1 --retries=0` passed 19/19. `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check`, and `npm run test:unit` passed after the cleanup. The seeded reducer campaign rerun reached 55 victories, 45 losses, turns 11–40, and zero save failures in 100 runs. The independent review is summarized below.
- Independent Market cleanup review at `78F5EA16…` accepted the direct invented/duplicate synergy guard and dead TradeTab removal. Tester exercised legal earned Wool/Grain sales and Salt/Tools/Spices buys at 320/390px; all 12 sampled quantity controls were at least 44×44px, no horizontal overflow occurred, and the saved balances matched posted prices. Tester and grader opened full-resolution images and found P3 zero spacing between a 320px Livestock count and its quote; tester also found P3 ambiguous `1`/`5`/`All` accessible names; grader calculated P3 3.20:1 contrast for 12px red buy prices. Reports: `artifacts/v2/{tester,grader}-market-cleanup-review/`. A browser regression first failed waiting for a descriptive sell label. The active Quick Trade now stacks count below name, keeps at least an 8px count-to-quote gap at 320px, names each action with good/quantity/total, and uses `#e06058` red with calculated 5.13:1 contrast on `#1a1610`. The focused browser case verifies spacing, labels, contrast, no overflow, and actual bonus sale; it passes. The complete adjacent 19/19 Market/Feast/save browser run, 80/80 units, typecheck, lint, build, and diff check pass after the fix. Opened new 320px top and buy-section captures in `test-results/market-haggle-v2-earned-tr-542dc-and-Quick-Trade-sale-quotes-gameplay/`. At exact frozen fingerprint `4301A444…`, both reviewers accepted all three P3 repairs. Tester measured a 23.17px Livestock gap at 320px and verified actual buy/sell debits and credits; grader independently measured 5.13:1 red price contrast, full action labels on all 57 buttons, 44px minimum targets, and no overflow. Each reran focused Market Playwright 6/6 on isolated ports. See `artifacts/v2/{tester,grader}-market-cleanup-retest/`. This is a scoped Market verdict.
- The authored Market module was renamed from `.js` to `.ts` and now checks the local/foreign merchant fields, valid resource IDs, seasons, event effects, forecast input, and injected picker draw callback. All four import sites were updated; no `data/market.js` references remain under source/tests. JSON-serialized nonfunction export SHA-256 stayed `3133756f921d7279dc2bed6824e35e396ca92202758cee3dcdd5599287b5c217` before/after, including all merchant/narrative strings and configuration. `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test:unit` (80/80), `git diff --check`, the 19/19 Market/Feast/save browser suite, and 100 seeded campaigns (55 victories/45 losses/zero save failures) pass. At fingerprint `6C81C424…`, tester found zero mismatches in 2,400 event, 1,200 forecast, 103 reputation, and 117 seasonal offer cases; grader independently matched all exported values, 108 picker and 45 forecast cases, and confirmed invalid resource/season/effect keys fail typechecking. Both found no scoped P0–P2. Grader found G-MTS-01 P3: README/CLAUDE named the deleted `.js` file; both references now say `market.ts`, and grader accepted the exact doc retest at `C297D899…` (`artifacts/v2/grader-market-ts-doc-retest/report.md`). Reports: `artifacts/v2/{tester,grader}-market-ts-review/`. At this content-only checkpoint, the active view and reducer remained unchecked JSX/JS; the view conversion is recorded below.
- The active view was renamed to `MarketSquare.tsx`; the component now types its market state slice, action dispatches, merchant display variants, pending bargain, resource/price tables, and local mode/selection. TypeScript exposed two unused imports and nullable price arithmetic; the imports were removed and the existing 0d fallback made explicit. README references now point to `.tsx`. Local `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test:unit` (80/80), `git diff --check`, the 19/19 adjacent browser suite, and 100 seeded campaigns (55/45/zero save failures) pass. Opened the fresh 320px Quick Trade screenshot at `test-results/market-haggle-v2-earned-tr-542dc-and-Quick-Trade-sale-quotes-gameplay/quick-trade-top-320-after.png`; the 23px count/price spacing and 44px buttons remain. Independent TSX/gameplay review pending. `App.jsx` and `gameReducer.js` remain unchecked, so this is a typed view boundary rather than full application conversion.
- At frozen Market view fingerprint `DF9A4FFE…`, tester and grader found no scoped TSX conversion regression. Tester replayed merchant and Quick Trade buys/sells, pending bargain save/load, and four byte-identical 320/390px Quick Trade screenshots; grader independently ran the focused suite and measured current UI. Their P3s were a compile-time view contract that accepted invented goods/merchants, inherited 2.94:1/3.20:1 small red prices in the hub/stall, and inherited 36px merchant 1/5 actions named only by quantity. Reports: `artifacts/v2/{tester,grader}-market-view-ts-review/`. Exact immediate-preconversion JSX source was not archived, so line-by-line source parity is not claimed. A new 320px browser test first failed on the 2.94:1 quote and then on a 36px mobile CSS override before passing. The current view types authored goods/merchant IDs, renders brighter buy quotes, wraps narrow stall rows, and gives merchant 1/5 buttons 44px targets and trade-specific names. The grader's original negative TypeScript fixture now produces five errors for invented merchant/resource/price keys; a negative numeric quantity still typechecks and is rejected by the reducer at runtime. Current `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test:unit` (80/80), `git diff --check`, and 20/20 adjacent Market/Feast/save browser tests pass. `node --experimental-strip-types artifacts/v2/seeded-campaigns/simulate.mjs`: 100 campaigns, 55 wins/45 losses, turns 11–40, zero save failures. The independent Market export comparison still matches SHA-256 `3133756f921d7279dc2bed6824e35e396ca92202758cee3dcdd5599287b5c217`. I opened full-size 320px current hub, Agnes buy, and Edmund sell captures under `artifacts/v2/market-stall-fix-captures/`; brighter quote text and two-line narrow stall layout remain within the viewport. Both reviewers accepted exact fixes at `42D42D9E…`: 4.716:1 board contrast, 5.131:1 stall contrast, 44px named actions and correct settled trades at 320/390px, no overflow/errors, 7/7 focused Market browser tests. Tester found Quick Trade images unchanged; grader rejected the original invalid TypeScript fixture and confirmed negative quantity is a reducer runtime invariant. Reports: `artifacts/v2/{tester,grader}-market-view-ts-retest/`. Full-game typing and screen-reader speech remain unreviewed.
- `data/economy.js` was copied to ignored `artifacts/v2/economy-ts-preflight/economy-before.mjs`, renamed to checked `economy.ts`, and all active source/test import paths were updated. The first comparator matched all 30 exports and 28 nonfunction values with pre-conversion SHA-256 `9355a11caeec6a271e4d90f737ac683da815a428adde51423674a0fadd1af354`; tester/grader independently confirmed conversion parity at `8C7484D6…`. Grader identified G-ECON-01 P2 (Estate displayed 45 need versus Easy Spring simulation 23; Normal Spring 41 versus 29; Hard Winter 37 versus 41), G-ECON-02 P3 (Spring ×0.5 prose versus ×0.6 rule; Winter +25% prose versus +10% pre-cap rule), and G-ECON-03 P3 (mutable resource groups and arbitrary generated-price keys). The 390px browser forecast regression failed all three initial cases, then passed after Estate and simulation shared `getSeasonFoodRequirement`. Two authored descriptions intentionally changed; `compare-corrected.mjs` verifies all other exported values and 104 price/draw cases, four invalid draws, and ten condition thresholds remain unchanged. Corrected exported-value SHA-256 is `07143d975ce10cad152cda203bd7bd32e845974f46355704d518d8a74397f0e4`. `verify-seasonal-parity.mjs` confirms all six original Easy/Normal/Hard Spring/Winter demand and simulator report cases unchanged. Grader's original `type-gap.ts` now fails with three expected diagnostics, rejecting `push` on readonly resource groups and an invented buy-price key. Two new food units and one resource cross-reference unit pass. The initial 38-case adjacent browser run passed 37; the Feast case read its tab coordinates before the existing resize listener aligned the active tab. Waiting for that layout postcondition made Feast 2/2 and the strict-port 5186 adjacent run 38/38, one worker, zero retries. `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check`, 83/83 units, and 100 seeded campaigns (55 victories, 45 losses, zero save failures) pass. Opened original and corrected Easy 390px images, plus corrected Normal Spring and Hard Winter images, show the numbers and guidance in place. At that checkpoint, independent retest remained pending; the later retest is recorded below. Most engine/UI JS and JSX remains unchecked.
- Fresh `npx playwright test tests/e2e/gameplay/auto-playthrough.spec.js --project=gameplay --workers=1 --reporter=line --grep 'Full game: (Easy/Passive|Hard/Passive)'`: 2 passed in 3.7 minutes. Easy/Passive reached a real browser victory at turn 40 in 150.4 s; Hard/Passive reached famine loss at turn 18 in 71 s, then restarted to Turn 1. Both asserted a terminal screen and no collected errors/page errors. Turn logs: `artifacts/v2/asserted-campaign-final/results.json`. They remain unseeded; the legacy turn helper increments per call and does not assert visible turn advancement after every call. Broader strict browser campaigns remain open.
- `node --experimental-strip-types artifacts/v2/seeded-campaigns/simulate.mjs`: 100 deterministic reducer campaigns across Easy/Normal/Hard and passive/agrarian/defensive policies; 55 victories, 45 losses, terminal turns 11–40, 2,069 accepted management actions, zero save failures. The harness rejects ambient `Math.random`, input mutation, nondeterministic replay, invalid core resource bounds, duplicate building IDs, and unexpected/stalled phases. Results are in `artifacts/v2/seeded-campaigns/results.json`. This tests a sampled scripted policy, not player UI or all 91 reducer commands.
- A prior asserted Easy/Passive 40-turn victory and Hard/Passive turn-23 loss/restart were also run before the selector and navigation changes. Their saved results are in `artifacts/v2/asserted-campaign-easy-passive/` and `artifacts/v2/asserted-campaign-hard-passive/`. Test-generated tracked reports/screenshots were restored after each campaign.

- Economy correction retest at frozen `6C71F1CE…`: tester and grader independently accepted G-ECON-01/02/03. Each ran 18/18 focused browsers and 83/83 units; tester checked the exact food warning threshold and a legal turn/save/load, while grader checked 18 ample/short/empty-stock scenarios, data/type parity, and opened desktop/mobile comparisons. Reports: `artifacts/v2/{tester,grader}-economy-forecast-retest/`. Both found adjacent P3 Estate mobile overflow (document width 428px at a 390px viewport, from a forced two-column Build New grid); grader also found P3 Chronicle wording that called a 36-food deficit `36 families went hungry` for 18 families. Exact layout regressions failed at 320 and 390px before repair, and the shortage unit reproduced the Chronicle line. Removing the phone-only grid override gives one-column Build New cards within 320/390/480px; the report now says `Shortfall: 36 food.` A unit verifies the reducer's Chronicle entry. Opened corrected 320/390px Build New captures under `artifacts/v2/estate-layout-green/`. After repair, the strict-port 5186 adjacent browser run passed 41/41, 84/84 units passed, typecheck/lint/build/diff check passed, and 100 seeded campaigns retained 55 victories, 45 losses, zero save failures. At exact fingerprint `52F9750A…`, tester and grader independently accepted both P3 repairs. Tester measured all 16 card/button bounds at 320/390/480px and completed a real 390px Strip Farm build (700→620d, 4→5 buildings); grader also checked 1366px retains three columns and the original Winter shortfall keeps population 13, garrison 1, food 0, and zero simulation draws. Each ran 6/6 focused browsers; grader ran 84/84 units, both passed typecheck, and neither found a new scoped P0–P3. Reports: `artifacts/v2/{tester,grader}-economy-layout-retest/`. The first broader 38-case run passed 37 due to a Feast test reading tab coordinates before its existing resize listener; waiting for the actual layout postcondition made Feast 2/2 and the adjacent run 38/38 before the P3 fixes. Neither review is a whole-game verdict.

- The active `src/data/buildings.js` registry was archived to ignored `artifacts/v2/buildings-ts-preflight/buildings-before.mjs` and converted to checked `buildings.ts`. Five source imports and README/CLAUDE references now name `.ts`; `buildingActions.ts` uses the registry's definition type. `compare.mjs` confirms the six runtime exports, 17 definitions, category groups (4/6/4/3), array order, and all authored strings/numbers have the same JSON SHA-256 `7c848993408e22bb783f8a30aeb6518efe8aff353cbf1d0877aba7c9715795ad`. A type-level check rejects unknown IDs in keys, prerequisites, upgrades, and building synergy references. The negative fixture produces four expected errors for invented building/resource/reference IDs. The initial 85/85 units, strict-port 5186 adjacent 34/34 browsers, and 100 seeded campaigns passed. The `game-data` production bundle hash remained `BV6ua7jD` across the preceding economy and building builds. At frozen `9A19E70F…`, tester found no scoped registry issue after 19 browsers; grader's independent review found G-BLD-01 P2 (unknown saved building occupies an inaccessible plot) and G-BLD-02 P3 (old Estate tests clicked Fishpond, accepted a count tautology, or skipped plot verification). Red unit and browser cases reproduced the unknown object and string entries in v2/legacy saves. The validator and writer now reject unrecognized building types; two 390px browser cases confirm visible rejection, unchanged bytes, and no legacy-to-v2 write, while a known legacy string still imports. Seven revised Estate browser cases target Strip Farm, exact 700→620d cost, 4/24→5/24 plots, 40d demolition refund, and a validator-valid insufficient-funds save. Current typecheck/lint/build and 86/86 units pass; 100 campaigns remain 55 wins, 45 losses, zero save failures. At `A71E0D54…`, tester independently passed 21/21 focused browsers, 86/86 units, and typecheck; grader passed 20/20 focused browsers, 17/17 save units, typecheck, and a negative BuildingId fixture. Both accepted G-BLD-01/02 as fixed with no new scoped P0–P3; reports and opened mobile/desktop images are in `artifacts/v2/{tester,grader}-buildings-ts-retest/`. Full gameplay browser run on strict port 5182, one worker, zero retries: 104 passed, one Market earned-bonus case timed out waiting for the title Load button after a blank-page render; its screenshot is under `artifacts/v2/buildings-ts-preflight/full-gameplay-output/`. The same Market case passed 1/1 in isolation on a fresh strict-port server. This first-attempt failure remains an open broad-suite stability observation. At this building checkpoint, the reducer and economy engine were still JavaScript.
- The seasonal simulator `src/engine/economyEngine.js` was archived at SHA-256 `BB0994B388EBE5C05293EF7B3A526D18ADB43A593D69FC5BB92E90EA73E58AE5` and converted to checked `economyEngine.ts`. `artifacts/v2/economy-engine-preflight/compare.mjs` matches 180 simulation results and exact random draw counts across three difficulties, three seeds, four seasons, five estate/stock cases; 18 build eligibility cases and helper boundaries also match. Three negative type expressions reject invented season, tax rate, and inventory resource. The preexisting save `Inventory` type omitted salt/tools/spices although the simulator consumed them. It now requires the 17 base resources with three optional purchased provisions. A red unit repro showed that negative salt and an invented tax rate were admitted; the validator now rejects invalid present salt/tools/spices quantities and unknown tax rates while retaining valid older saves. Local static checks, 88/88 units, 100 seeded campaigns (55/45/zero save failures), and a first-attempt 56/56 adjacent Estate/Market/save/season/multi-turn browser run on isolated strict port 5182 pass. Two new 390px browser cases verify visible save rejection, unchanged legacy bytes, and no v2 slot. Independent tester/grader review of this economy slice is pending.
- At economy fingerprint `63480C77…`, both reviewers independently found that a validator-accepted extra `inventory.dragon_eggs` key changes storehouse capacity and building production while the item is invisible to the player. Tester T-EENG01 used +100 to suppress output in a 390px Market/season path; grader G-EENG-01 used -100 to create free space in a 300/300 store, yielding 5 coal, 3 leather, and 3 wood at 390/1366px. The archived engine has the same numeric behavior; the defect belongs to the save boundary. The grader's separate 1,200-case simulation/16,800-helper comparison found zero output/report/draw differences. Red unit and mobile browser cases reproduced the accepted unknown key. Save import/write now checks each inventory key against `RESOURCE_CONFIG`, retaining the 17 required and three optional authored quantities; the `Inventory` type moved from save to `data/economy.ts`. The exact red cases pass. Current local checks: typecheck/lint/build, 89/89 units, 57/57 adjacent browsers on an isolated strict server with zero retries, 180-case parity, and 100 campaigns (55/45/zero save failures). The original reviewer browser probes need exact retest; reports: `artifacts/v2/{tester,grader}-economy-engine-ts-review/`.
- Exact economy retest at frozen `9BE56B6E…`: tester's original +100 phantom item and -100 variant reject visibly on 390px v2 Load and legacy Import; grader reproduced -100/+100 rejection at 390/1366px. Both confirmed unchanged original bytes, no v2 slot from a failed legacy import, and a playable title. Valid older and optional-provision imports, legal production, full-store season progression, and a pending choice/save continued. Tester ran 19/19 strict focused browsers, 89/89 units, and typecheck; grader ran 19/19 browsers, 20/20 save units, typecheck, and archived 180-case parity. Neither found a new scoped P0–P3. Opened full-size mobile alert and desktop legal-choice screenshots; reports: `artifacts/v2/{tester,grader}-economy-engine-ts-retest/`. This accepts the scoped P2 repair, not full nested-save or whole-game 2.0 completion.

## Independent findings and visual evidence

The tester and grader found and retested malformed-save acceptance (T-B01), fabricated pending events (G-S01), offscreen error feedback (G-S02), corrupted known-event effects (G-R2-01), missing consequence names (G-R2-02), mobile first-viewport crowding (G-R2-03), missing flip/raid phase payloads (T-R3-01), altered finite authored event effects (T-R3-02), and legitimate fractional raid result rejection (G-R4-01). Exact reproductions, evidence, and scoped verdicts are under `artifacts/v2/tester-*` and `artifacts/v2/grader-*`. The grader independently confirmed G-R4-01 fixed in `grader-retest-5`: accepted warning, reducer-produced result, legacy read, v2 write/read; tested ratio bounds. No unresolved P0/P1 is known in these targeted paths. Comprehensive nested save shapes and every phase/branch remain unverified.

The tester and grader found T-F01 (transparent raid result panel), T-F02 (zero-garrison message incorrectly credits soldiers), and T-F03 (stale contributor guidance), all P2 at fingerprint `DD187AB7…`. The panel now uses an opaque gradient and the message credits fortifications. Both reviewers independently retested T-F01/T-F02 at `35D5B555…` and accepted the opened full-size screenshots and browser continuation. They found remaining documentation claims about 5173, outdated 25/50 raid thresholds, and automatic zero-garrison loss; CLAUDE/README now state the actual 5180 worktree port, 18/38 thresholds, and possible fortified zero-garrison victory. The later scoped grader review accepted these rule corrections. Evidence: `artifacts/v2/raid-result-after/`, `tester-p2-retest/`, `grader-p2-retest/`.

At saved-PRNG fingerprint `A7BC9A72…`, the independent tester found that Horizon Scan rendered no anomalies (T-RNG-01, P1) and replayed different ambient-random scans (T-RNG-02, P2). The grader reproduced those defects and found reducer-produced terminal states rejected by the save validator (G-RNG-02, P2), fabricated/repeated scan rewards (G-RNG-04, P2), unreachable Eagle-eyed rating (G-RNG-05, P2), and stale fixed-draw parity evidence (G-RNG-06, P3). The primary repaired these with a typed seeded planner/scorer, canonical reducer rewards and guards, reachable all-five rating, and structured terminal reason validation. Both reviewers independently accepted those original findings as fixed in tested paths at `DA8EFCB6…`; see `artifacts/v2/tester-random-state-retest/` and `grader-random-state-retest/`. They found two new P2s: overlapping 32px scan targets for a valid seed, and a five-find acknowledgement button clipped by 20px in the first 1366×768 viewport. The primary placed anomalies in five shuffled horizontal lanes and put long observations in a labeled scroll region while keeping the reward and action visible. At `D22069EE…`, both reviewers independently retested the exact overlap seed and five-find viewport: normal Dust/Birds pointer clicks succeeded, the 40px action occupied y674.8–714.8 within 768px, and all five findings remained reachable by inner scroll. No new P1/P2 in these scoped paths. See `artifacts/v2/tester-watchtower-layout-retest/`, `grader-watchtower-layout-retest/`, and opened primary captures in `watchtower-final/`. The grader inspected but did not drive the 1280×720 and 390×844 report captures.

At first initial-state fingerprint `C62B09E4…`, the tester and grader found that family bonus objects remained shared across games, and the grader found injected market draws accepted NaN/out-of-range values. Exact probes are in `artifacts/v2/tester-initial-state/` and `grader-initial-state/`. The primary now clones each nested family bonus and rejects draws outside finite `[0, 1)`; new unit regressions and all static/focused checks pass. Both reviewers independently confirmed their exact reproductions fixed at fingerprint `130C0DB6…`; reports are in `tester-initial-state-retest/` and `grader-initial-state-retest/`. The tester's browser replay evidence applies to the earlier parity-equivalent initial-state fingerprint; the final guarded-draw and clone corrections had unit and independent probe coverage, not a repeated browser campaign.

The primary's Gambit red regression showed that the old `TAVERN_GAMBIT_RESULT` command could credit a forged 1,000,000 denarii win. The new reducer-owned seeded command derives the opponent, outcome, earnings, and next RNG state after validating phase, choice, wager, seed, funds, and five-round limit. Four unit tests and the original two browser tests passed. Opened `artifacts/v2/gambit-browser/` screenshots show the first result at 1366×768 and choice/result at 390×844 after correcting the displayed round and card width. At exact fingerprint `74F65B5B…`, the tester and grader independently accepted sampled reducer guards/rule parity but both found a fourth-round continuation defect; both reproduced a pending wager settling after Load during the reveal. The tester ranked Load as P1 because the just-restored state changes, while the grader ranked it P2. Reports and screenshots are in `artifacts/v2/tester-gambit-review/` and `grader-gambit-review/`. The primary now unmounts transient Map stations on successful load/import and cancels Gambit's reveal timer on unmount; the result screen offers another round until five have actually completed. Four Gambit browser tests now pass, including the exact two regressions. At exact fingerprint `0F5D7B36…`, both reviewers independently accepted those original browser reproductions as fixed: the saved 700d/round-zero game persisted beyond the reveal timer, and five rounds played directly with no sixth option. Opened screenshots and scripts are in `artifacts/v2/tester-gambit-retest/` and `grader-gambit-retest/`. Neither report is a whole-game signoff.

The cellar preflight `artifacts/v2/rats-preflight/probe.mjs` showed the old `TAVERN_RATS_RESULT` accepted a fabricated million-denarii reward and negative million-food loss twice. The new `TAVERN_RATS_FINISH` ignores caller-supplied payout fields, derives a bounded rating and food loss from the seeded schedule and catch/escape counts, enforces once per season, and rejects stale seeds; five unit tests cover exact examples, the forgery, invalid actions, and present Gambit/cellar save counters. The original commit was served from an ignored archive on port 5183 for controlled intro/result captures in `artifacts/v2/baseline-rats/`; it was not edited, and the temporary server was stopped. Candidate intro captures at 1366×768 and 390×844 plus result captures at 1366×768, 1280×720, 1920×1080, and 390×844 in `artifacts/v2/rats-browser/` were opened and compared. Instructions and result values are more legible, the result action is visible on phone without horizontal overflow, and the baseline's phone result stacked three stats vertically. At frozen fingerprint `93A2F78E…`, the tester found that overlapping scheduled rats erased five escapes from a normal seed-1 run, and the grader found invisible keyboard focus on active grid cells. Both independently reproduced a bounded direct-action finish claiming 15 catches without play; the tester ranked it P2 and the grader P3. The primary settled replaced rats, accounted for all 15 planned spawns at time-up, and restored the global focus outline. The full focused run, 49 units, typecheck, lint, and build passed. At exact fingerprint `DB0268FC…`, the tester independently repeated the real 390×844 20-second run: one catch and 14 escapes, 28 food charged, saved 452 food and 322 grain, loaded seasonal lockout, no page/console errors. The grader verified a visible solid 3px gold focus ring and opened the full-size mobile result; no new P1/P2 in inspected paths. Reports: `artifacts/v2/tester-rats-retest/` and `grader-rats-retest/`. Catch counts remain self-reported by this client-only game; bounded, deterministic reducer scoring does not attest human input or prevent direct action fabrication.

The stranger trade preflight showed the old `TAVERN_STRANGER_TRADE` accepted `cost: -1_000_000` and `foodReward: 1_000_000`, taking an Easy start from 700d/480 food to 1,000,700d/1,000,480 food without an encounter. The red regression failed before repair. The reducer now draws the encounter on `TAVERN_VISIT` from the saved cursor, persists a pending type, enforces the authored 150d-for-10-food terms and one resolution, and validates pending encounter saves. Decline previously hid the card locally without dismissing it in state; it now clears the encounter. At seed 2, three new units and two browser flows pass, including pending offer reload, canonical trade, completed reload, and decline. An immutable baseline archive served on temporary port 5183 for controlled `artifacts/v2/baseline-stranger/` captures; it was stopped. Opened 1366×768 and 390×844 baseline/candidate screenshots show the earlier card below the first laptop viewport and with tiny, dim controls; the candidate puts the decision before stations, spells out the food quantity, and keeps the action in the first viewport at both sizes. Because visiting now consumes a saved encounter draw, the previous seed-1 Gambit/Rats browser expectations changed: two wagers are defeats (650d, RNG `3975261312`) and the one-catch cellar run plans 16 rats (15 escapes, 30 food lost, RNG `3087660854`). The combined seven-test Gambit/Rats/stranger browser run passed after updating those exact expectations; the latest 390×844 cellar result screenshot was opened. At exact fingerprint `58E03B89…`, both reviewers confirmed the forged-trade closure, canonical keyboard/browser trade and decline, and save behavior. They found that a later mobile CSS selector overrode the intended 44px touch size to 38px (tester P2, grader P3); the grader also found that declining provisions wrote a counsel chronicle line (P3). The primary set an explicit 44px minimum on each offer action and made the trade-decline line accurate. A red chronicle regression, 52/52 units, static checks, build, and both focused browser flows passed. At exact fingerprint `2CEE84FA…`, both reviewers independently accepted the original touch-size and chronicle retests: 44px measured actions, correct saved provision-decline line, visible keyboard focus, no browser errors or new scoped P1/P2. Reports: `artifacts/v2/tester-stranger-review/`, `grader-stranger-review/`, and matching `*-stranger-retest/` directories.

The Bard preflight reproduced an unlimited +10d payout from the old `TAVERN_BARD_RIDDLE_SOLVED` action. Content selection and option order used ambient `Math.random`, and the active riddle lived only in component state. Five stable riddle IDs and a typed content selector now use the saved random cursor, persist the selected content and option order, cycle authored tales through a no-repeat bag, and verify a correct displayed answer before a once-per-ID +10d award. The old action is inert. Four new unit tests cover the exploit, seeded mix and tale cycling, repeated reward after save, and malformed saved Bard fields. At seed 5, the real-browser riddle, answer, pending and answered save/reload, ten further reveals, and repeated answer passed with 710d unchanged on the repeat. Baseline source from the immutable archive briefly ran on 5183 for matched map-riddle captures and was stopped. Opened original/candidate 1366×768 and 390×844 images in `artifacts/v2/baseline-bard/` and `bard-browser/` show the authored question and choices preserved. At `B1B112CA…`, the tester's adversarial QA found no scoped P0–P2; the grader found G-BARD-01 P2, a solved riddle pinned on return, and G-BARD-02 P3, a dim correct-answer explanation. A new browser assertion reproduced G-BARD-01. The component now draws new content on entry after settled or non-riddle content and keeps an unanswered riddle stable. The fresh 390×844 reentry capture was opened and shows a different tale; the answer text is 16px with calculated light-green/panel contrast about 10.4:1, up from 3.8:1. The focused browser and frozen eight-flow Tavern set pass with these corrections. The pending-riddle cursor assertion initially failed because it compared before the lazy Tavern mounted; the test now waits for the station and compares cursor before and after Bard entry. At `1D09BB9E…`, both reviewers independently accepted exact reentry and contrast retests with no new scoped P1/P2; see `artifacts/v2/tester-bard-retest/` and `grader-bard-retest/`. Whole-game approval remains open.

Marta/Aldric preflight: direct `TAVERN_MARTA_ACCEPT_OFFER` with `storage_deal` increased capacity by 20 and debited 50d before any offer appeared; the red regression caught it. The shared typed selector now persists each companion's displayed content and no-repeat advice/story bags, drawing offers from the saved cursor at the authored 25/35/40 proportions. Accept/decline require the exact unresolved offer ID and record the result; malformed offer IDs, inconsistent resolved histories, and invalid queues are rejected at the save boundary while older saves without the new optional fields remain readable. Seed 4 produces Marta's Storage Expansion and Aldric's Garrison Morale from a normal Tavern visit. Six unit cases and four companion browser flows cover those examples, save/load, duplicate/forged commands, advice/story cycling, and invalid saves. Both offer action pairs measured 38px initially and now pass a 44px mobile target assertion; cost/effect text is larger. The original archive ran briefly on 5183 for controlled offer captures and was stopped. Opened original/candidate 1366×768 and 390×844 offer images in `artifacts/v2/baseline-companions/` and `companions-browser/` show the same authored prose and improved first-viewport visibility. Aldric's recruitment display now says one man-at-arms, matching the reducer's existing single recruit. At frozen fingerprint `7A1BFE51…`, the independent tester found no scoped P0–P2, while the grader found inherited G-COMP-01/02 P2: paid Basic Drill had no combat effect and Recruit Referral exceeded the 25 overall/ten men-at-arms limits. A transient focus-style read was retracted after a waited keyboard probe showed a visible 3px gold outline. The primary reproduced both gameplay defects with red unit tests. Basic Drill now grants one flat defense per stationed soldier for three season simulations, including a raid on the final season. Normal and referral recruitment share a 25 overall, ten men-at-arms, and 60%-of-population capacity rule; the full-roster card explains the limit. A reducer-produced turn-16 warning survived save/read with the third-season bonus, and the 390×844 raid warning and full-roster offer screenshots were opened. Three raid and four companion browser tests pass, including a 20-vs-18 drill victory and absence of Accept at full capacity. At `17B55971…`, both reviewers independently confirmed G-COMP-01/02 fixed in scoped reducer, save, and mobile browser checks. The tester found T-COMP-R1 P3: the blocked referral copy omitted the 60%-of-population limit. The card now lists all three recruitment limits; the 390×844 population-cap screenshot was opened and its browser assertion passes. At `3C4BB41B…`, both reviewers accepted the exact population-cap copy retest: no Accept button, correct 60% explanation, no horizontal overflow, and first-viewport actions visible. Tester reran the four companion browsers 4/4; see `artifacts/v2/tester-companions-copy-retest/` and `grader-companions-copy-retest/`. This is a scoped companion verdict, not whole-game approval.

The two near-duplicate Marta/Aldric JSX views were replaced by a typed shared component with character-specific palette, copy, offer labels, and 44px actions. The six existing offer/cap tests were retained; four browser cases now assert seeded advice and story content, labels, and usable mobile controls. Pending Marta offer resume and Aldric resolution still pass. The shared-view browser suite passed 8/8 initially, then 10/10 after the first-visit mobile corrections; the adjacent Tavern suite passed 18/18. The primary opened the four 390×844 advice/story captures and the 1366×768/390×844 offer captures, comparing offers to the prior review images. The new optional Scribe-note save flags reject malformed values and older saves lacking them still load. At exact fingerprint `1F62104E…`, the independent tester and grader found no scoped P0–P2. Tester T-CV01 P3 captured Aldric title at y188 behind navigation ending y238 on first mobile entry; grader G-CVIEW-01 P3 measured 12px italic first-visit notes. A red browser regression reproduced the title occlusion. Tavern station changes now reset scroll and notes render at 14px; opened current 390×844 Marta/Aldric first-note screenshots show titles and dismissal controls within the viewport. The original focused regression, 10 companion browsers, adjacent 18 Tavern browsers, 62 units, static checks, and build pass. At exact fingerprint `C66BA1FF…`, both independent reviewers accepted the two P3 retests: Aldric title y272 with navigation bottom y238 and scrollY=0; both notes 14px/22.75px; 44px dismissals visible and keyboard usable. No new scoped P0–P2. Reports and full-size mobile captures are under `artifacts/v2/tester-companion-view-retest/` and `grader-companion-view-retest/`.

Feast preflight: the original `HALL_FEAST_COMPLETE` command accepted caller-supplied effects, and a red unit command raised all four Hall meters to 100 with a forged million-effect payload. The original component also used ambient `Math.random` to choose an event on mount. A typed resolver now derives effects and the event from the selected authored IDs and the saved random cursor, and the reducer enforces one settlement per season. The save boundary checks unique, possible historical feast entries while admitting old effect-only entries. A real Chromium flow at seed 17 checks the proposal preview, pending save/load, unchanged preview, settlement, repeated command lockout, and exact saved state. A controlled original preview at the same three choices and event was captured on port 5183 and the server stopped. Opened 390×844 original/candidate preview and completed images show brighter event and completion copy, a usable Hall active tab, and first-viewport Feast title; the original preview event was nearly unreadable and its Hall nav hid tabs at that width. Full images are in ignored `artifacts/v2/baseline-feast/` and Playwright `test-results/`. The authored `FEAST_DATA` object matches archived baseline JSON, including all six events. At exact frozen fingerprint `E318DBFC…`, tester and grader independently found P1 T-FEAST-01/G-FEAST-01: a validator-accepted older Hall state with no feast history crashed on settlement. Both also found inherited P3 T-FEAST-02/G-FEAST-02: selected options had no accessible state. The grader's direct probe and tester's real 390px blank-screen capture are under `artifacts/v2/{grader,tester}-feast-review/`. A new unit reproduced the P1 before the reducer used an empty array for missing history, and a new browser case now settles that same accepted shape. Option buttons now expose `aria-pressed`; the existing browser planner asserts true/false states. At exact retest fingerprint `66685C38…`, both reviewers independently accepted both fixes: legal settlement and save/reload from the older shape, one canonical history row, no blank/error, pressed state announced, and visible keyboard focus. The tester's 390px browser suite passed 2/2; the grader reran the direct probe and five focused units. No new scoped P0–P2; broader Hall and full-game gates remain open. Reports are in `artifacts/v2/{grader,tester}-feast-retest/`.

Market preflight: `HAGGLE_START` accepted invented and mismatched merchants; `HAGGLE_COUNTER` accepted a counter as large as `1e308`; the save boundary admitted forged pending offers and `1e308` seasonal prices. Red unit probes reproduced these failures. The typed `marketHaggle.ts` guard now checks the current season's local/foreign merchant offerings, UI counter range, saved pending status/round/difficulty/current offer and opening quote, and exact price-key sets within the authored ±20% generation range. The reducer checks these terms before countering or settling; direct invalid commands leave state unchanged. One normal local sale and a seasonal foreign purchase start and roundtrip; a legal counter retains a savable result. The 390px browser path exercises a real Edmund sale through Save/Load and exact denarii/inventory effects. A new assertion reproduced the merchant title at y60 behind navigation bottom y238 after opening a saved bargain. Market station and bargaining changes now reset scroll; the opened candidate pending view shows Edmund's title and bargaining terms, while the controlled original capture at `artifacts/v2/baseline-market/` shows the title hidden at y22 (mobile) and y35 (desktop). Candidate 1366×768 and 390×844 pending/settled images were opened from `test-results/`; the original archive server was stopped after capture. Market authored IDs and definitions were not edited. The first independent review and primary repairs are summarized above; reviewer retest is pending. The validator checks plausibility of prices and pending terms, not cryptographic authenticity of client-owned saves; other root state fields still require deeper validation.

`raidEngine.js` is now `raidEngine.ts` with explicit raid state/result contracts and an optional validated random draw. The grader's independent baseline-vs-TypeScript sweep found zero JSON-normalized differences in 5,472 trigger/resolution cases; 648 cases changed only negative zero to positive zero for no soldier losses. The tester independently exercised warning→result→continue and save/reload on reducer-produced fractional outcomes. The grader found an inherited P2 contradiction between zero-garrison partial-defense UI copy and the authored "unopposed" line, plus a P3 active-raid type gap. The primary added dedicated fortification victory/partial-defense lines, retained "unopposed" for zero defense, tightened the active-raid union, and revised the no-garrison dashboard warning. Both reviewers confirmed the exact narrative/type fixes at `AA92857A…`. The grader then found that fortified victory skipped one random draw; the primary restored the legacy two-draw count and added a unit regression. Both reviewers accepted that exact retest at `135B2A2F…`; the grader compared 72 further cases without draw-count or numeric-mechanics mismatch. Mechanics still match `artifacts/v2/raid-engine-preflight/scenarios.json` after excluding the two intentional narrative fields. Three browser raid flows pass, including the Aldric drill warning; the new zero-garrison partial screen was captured and opened at `artifacts/v2/raid-narrative/zero-garrison-partial.png`. Fixture-driven paths do not establish natural raid reachability.

Opened baseline and candidate images cover title and nine tabs at 1366×768 and 390×844; actual seasonal choices were captured only on the candidate because the baseline capture landed during transition. `artifacts/v2/candidate-ui-3/` adds inspected 1280×720 and 1920×1080 title/Estate/seasonal views and targeted Hall/Chapel/Forge captures, plus a 390×844 phase-scroll before/after image. The initial seasonal screenshots in that folder were dim transition frames; they were recaptured after the Chronicle state settled, and only the latter support visual observations. Full deep interaction, outcome, keyboard/focus, browser zoom, and approved Windows golden coverage remain open. Screenshots are qualitative layout evidence, not proof of gameplay correctness.

## Code census and open gates

Count physical UTF-8 lines, including comments and blanks, from baseline Git blobs and current tracked/untracked first-party files; exclude dependencies, generated output, and ignored artifacts. Source categories: implementation `src/**/*.{js,jsx,ts,tsx}` excluding `src/data`; data `src/data` code files; styles `src/*.css`; executable tests `tests/**/*.{js,jsx,ts,tsx}`; tooling the five selected config/playtest scripts; documentation all `.md`; other text `.json`, `.html`, `.yml`, `.yaml`, `.txt`, `.css`, `.svg` outside preceding categories. Generated tracked QA JSON is included on both sides after restoring it. Type-only modules: zero on both sides; mixed TS implementation counts as implementation.

| Category | Baseline | Candidate |
| --- | ---: | ---: |
| Implementation | 47 files / 30,642 lines | 59 files / 31,586 lines |
| Authored data | 20 / 13,566 | 21 / 13,849 |
| Styles | 1 / 1,139 | 1 / 1,164 |
| Executable tests | 17 / 3,585 | 52 / 6,909 |
| Selected tooling | 5 / 1,879 | 5 / 1,893 |
| Documentation | 9 / 3,089 | 14 / 3,289 lines |
| Other text | 6 / 8,382 | 9 / 9,455 |
| Inclusive text subtotal | 62,282 | 68,145 |

Production implementation plus authored data and styles is 45,347 baseline versus 46,599 candidate, an increase of 1,252 lines. Implementation alone increased 944. The required measurable total production reduction has **not** been achieved. BlacksmithTab, MapTab, Watchtower, and other large views remain. No content IDs were removed in the current diff, but complete reference validation is still open. First-party JS/JSX migration, complete nested save contracts, full visual/interaction review, and an independent final verdict on the completed design are not done. Six genuine browser campaigns (five turn-40 victories and one Hard famine loss/restart) passed in the prior 105-case run, which had one unrelated blank-load timeout; no full-suite pass is claimed.

Synergy preflight: the archived seven-path/21-tier JavaScript data and evaluator are in the source worktree's ignored `artifacts/v2/synergy-ts-preflight/`, with SHA-256 `B6C4B897…` and `E437F608…`. Before behavioral corrections, `compare.mjs` matched all four registry exports and 1,000 generated states across 21,000 tier-condition calls and 9,000 helper calls. Both reviewers confirmed conversion parity at `7FB3B66B…` and reproduced inherited rule, reward, notification, and saved-tier defects. The converted build passed typecheck/lint/build, 92 units, and independent 14-case Estate/Market browser runs. A redundant six-campaign run was interrupted before any terminal result and is not counted as a pass. Subsequent corrections are in this checkpoint and passed typecheck plus 95 unit tests in the source worktree; corrected-build browser and independent retests remain pending. See `checkpoint.md` for current repository verification.
