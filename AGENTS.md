# The Lord's Ledger 2.0 working guide

## Boundary and mission

This independent repository is `C:\Users\thecl\Documents\lords-ledger-typescript-rewrite` on `main`. It preserves the original Git history through `47570f9caa5696c7369d88a42ae87171b78cd68b` and a checkpoint of the uncommitted rewrite from `C:\Users\thecl\Documents\The-Lords-Ledger-v2`. The user authorized this checkpoint commit; the larger refactor goal remains paused. The original checkout at `C:\Users\thecl\Documents\LordsLedger`, its `main` reference, and the two existing rewrite worktrees are protected. Do not change those checkouts or their refs. See `docs/v2/checkpoint.md` for the current state; older evidence below describes the source worktree at its recorded fingerprints.

The mission is to preserve the 40-turn medieval estate game and all authored choices while repairing gameplay, improving readability and interaction, migrating first-party executable code to strict TypeScript, and removing redundant implementation code. Do not make the game easier by bypassing systems or trim narrative content to lower line counts.

## Actual commands and evidence

- Install: `npm ci` (Node 22.23.2, npm 10.9.8 at baseline).
- Development: `npm run dev -- --host 127.0.0.1 --port 5180 --strictPort`; verify the responding worktree before browser use. Give baseline and candidate runs isolated browser contexts and storage.
- Production build: `npm run build`. Lint: `npm run lint`.
- Existing test discovery: `npx playwright test --list` found 109 tests at baseline. Candidate `playwright.config.ts` uses an isolated server at `127.0.0.1:5182` by default (`LL_TEST_PORT` overrides it), strict port, and no server reuse. Run `npx playwright test --project=gameplay --workers=1` for browser gameplay, or `npx playwright test --workers=1` for all projects. The full suite takes about 21 minutes on this host and has no Windows visual goldens yet.
- Typecheck: `npm run typecheck`; unit: `npm run test:unit`; focused browser examples: `npx playwright test tests/e2e/gameplay/save-v2.spec.ts --project=gameplay --workers=1`, `building-v2.spec.ts`, `forge-purchase-v2.spec.ts`. `npm run build` and `npm run lint` remain separate checks.
- Evidence: `docs/v2/baseline.md`, `docs/v2/migration-ledger.md`, `docs/v2/design.md`, `docs/v2/verification.md`, and ignored `artifacts/v2/`.

## Feature and state map

- `src/App.jsx`: phase presentation, transient tab subviews, audio, and browser save I/O. Move validated persistence into a typed boundary; DOM and storage must stay outside simulation transitions.
- `src/engine/gameReducer.js`: durable game state and commands. `createInitialState(seed)` makes fresh startup/replay state and seeded market prices. `random.ts` carries a saved Mulberry32 cursor through reducer actions; game-affecting reducer helpers require explicit draws. `buildingActions.ts` owns building identity, plot counting, upgrade eligibility; `transactionValidation.ts` guards quantities; `eventSelector.ts` owns typed event filtering; `raidEngine.ts` owns typed raids; `watchtowerScan.ts` owns seeded scan plans and scoring. `tavernBard.ts` and `tavernCompanion.ts` select persistent dialogue/rewards/offers; `data/militaryRules.ts` centralizes recruitment capacity and the three-season Aldric bonus. Economy, perspective flips, synergies, and meter/legacy effects remain in neighboring modules. Season order and cross-domain actions must remain explicit and atomic. Remaining component-owned gameplay RNG and full player-flow replay still need audit.
- `src/data/`: authoritative definitions for resources, buildings, military, market, chapel, blacksmith, people, Hall disputes/audiences/decrees, tavern, watchtower, events, flips, raids, synergies, and endings. Preserve stable IDs and narrative branches. Validate references before removing compatibility paths.
- `src/components/`: nine management tabs plus Tavern, Watchtower, Great Hall subviews, minigames, event/raid/flip screens, notifications, tutorial, and outcomes. Presentation state should remain local unless saves intentionally persist it.
- Saves: `src/save/saveGame.ts` reads the legacy `lords-ledger-save` key only on explicit import and writes an envelope at `lords-ledger-v2-save`. Import leaves the old bytes untouched and does not overwrite an existing 2.0 slot. The current validator checks core fields, authored pending event effects, phase-specific flip/raid payloads, fractional raid defense ratios, structured terminal reasons, and unsigned saved RNG state. Legacy and earlier-v2 saves lacking RNG gain a deterministic derived seed. Many nested subsystem shapes still need full contracts and validation.

## Work loop and review

For each vertical slice, inspect definitions, consumers, save shape, and tests; characterize baseline behavior; implement one coherent change; run focused tests, strict typecheck, lint, build, and an affected player flow; inspect fresh screenshots; obtain independent tester/grader findings at meaningful checkpoints; then update the ledger. Do not edit Vite-served source during a browser campaign. Review reports apply only to their recorded source fingerprint, including uncommitted and untracked source.

The screenshot matrix uses 1366x768 as primary, 1280x720 for existing comparisons, 1920x1080 wide, and about 390x844 narrow. Cover title/navigation, all nine tabs, deeper interactions, phase/outcome screens, and stress states. Open actual before and after images and critique text, contrast, clipping, focus, and scroll; screenshot existence or pixel change alone is not approval. Update goldens only after the intended image was inspected and independently accepted. Browser screenshots do not establish gameplay correctness.

Tester and grader are independent reviewers. They may write only under assigned `artifacts/v2/` review directories and must not edit production or shared tests. Tester provides reproducible player-flow failures; grader inspects diff and actual images and marks uninspected areas `NOT REVIEWED`. Primary agent owns production changes and test integration.

## TypeScript and integrity gates

Final migration must include app, engine, content, tests, and executable tooling with `strict` and `noUncheckedIndexedAccess`; temporary `allowJs` is a migration aid only. Avoid blanket `any`, suppression comments, double assertions, and unchecked persisted-data casts. Use domain contracts and discriminated actions where they clarify legal transitions. Keep CSS, HTML, JSON, and assets in their native formats.

Do not treat an assertion-free campaign, a skipped save assertion, a passing build, or a generated screenshot as proof of correctness. Preserve content, legal choices, save integrity, deterministic replay, and realistic outcomes. No unresolved P0/P1, no significant new P2, complete interface review, genuine browser victory and loss/restart, and honest code-category metrics are required to call 2.0 complete.

## Current status and next slice

The Market haggling slice checks actual local/seasonal merchant offerings, counter ranges, pending terms, authored seasonal price ranges, and reputation in typed save guards. Authored goods without a generated buy key derive a merchant buy quote from the seasonal sell price; foreign premium demand is executable. Stall and reducer share quote and quick-sale bonus calculations; short-stock bargains refuse settlement. Tester and grader accepted malformed-synergy save rejection and brighter sale text at F16387D5…, then accepted the direct invented/duplicate synergy guard and unused TradeTab deletion at 78F5EA16…. They found three P3 active Quick Trade issues: a 320px zero stock-to-price gap, ambiguous quantity button names, and 3.20:1 small buy-price contrast. The current view stacks stock below the good, names quantity actions, and uses 5.13:1 red. Local browser assertions and opened 320px top/buy images pass; both reviewers independently accepted exact retests at 4301A444…, with a 23.17px count-to-price gap, 5.13:1 buy-price contrast, full action names, and 44px controls. A bounded client-edited accepted-status bargain remains a P3 authenticity limit. At that earlier checkpoint, 80/80 units, 19/19 combined Market/Feast/save browser flows, typecheck/lint/build, and 100 reducer campaigns passed. Major TypeScript migration and actual production reduction remain.

The Great Hall feast slice now derives the event and all effects from three authored choice IDs and the saved random cursor. It rejects forged, stale, duplicate, and out-of-phase settlement; compatible old effect-only histories load. A controlled original/candidate 390px comparison was opened. At E318DBFC…, both reviewers found an accepted older Hall save that crashed on Feast settlement (P1) and an inherited missing selected-option accessibility state (P3). Exact red regressions now pass after the reducer handles absent history and option buttons expose `aria-pressed`. Five Feast units make 67/67 passing, two Feast plus 11 save browser flows pass, and 100 reducer campaigns retain 55 wins/45 losses with zero save failures. Typecheck, lint, and build pass; both reviewers independently accepted the exact fixes at 66685C38… with no new scoped P0–P2. Production code is now 46,190 lines, 843 above baseline. Source migration and total reduction remain required.

Baseline commit, clean source, installation, test discovery, and primary screenshots are recorded in `docs/v2/baseline.md`. Focused verification is recorded in `docs/v2/verification.md`; inspect exact independent reviewer reports before claiming a gate. Earlier natural browser campaigns reached a 40-turn Easy victory and Hard famine loss/restart. An earlier full browser run recorded 103 pass, 13 missing-Windows-golden failures, and 1 skip; it predates recent changes. Market authored content is checked `data/market.ts` with unchanged exported-value hash; both reviewers accepted content/type parity at 6C81C424… and the path correction at C297D899…. The active `MarketSquare.tsx` conversion passed scoped tester/grader review at DF9A4FFE…; they found inherited small/ambiguous merchant buttons, dim buy prices, and a permissive view prop type. The correction gives merchant actions descriptive names and 44px targets, brightens small prices, and rejects invented resource/merchant IDs at compile time. Both reviewers accepted the exact P3 retest at 42D42D9E…: 44px named actions, 4.716:1 board and 5.131:1 stall buy quotes, legal trade results, unchanged Quick Trade, and no scoped new P0–P2. Resource/economy data is now checked `data/economy.ts`. At 8C7484D6…, the tester confirmed conversion parity and the grader found the inherited Estate food forecast mismatch, incorrect Spring/Winter guidance, and three permissive resource type expressions. A typed `foodRequirement.ts` now supplies both simulation and Estate; the two descriptions and type contracts were corrected. At that checkpoint, 83/83 units, 38/38 adjacent browser cases, typecheck/lint/build, and 100 campaigns (55 wins/45 losses, zero save failures) passed. The exact correction was independently accepted at 6C71F1CE…. Production reduction and most other JS/JSX typing remain open. Do not treat `allowJs` as completion.

At economy forecast fingerprint 6C71F1CE…, both reviewers accepted the P2 forecast and P3 guidance/type corrections in the inspected paths, then found P3 mobile Build New overflow and P3 food-shortfall wording. The exact red layout and Chronicle regressions now pass after the phone grid uses one column and the report names missing food units. Current checks: 84/84 units, 41/41 adjacent browsers, typecheck/lint/build, and 100 reducer campaigns (55 victories, 45 losses, zero save failures). Both reviewers independently accepted the exact P3 retests at 52F9750A… with no new scoped P0–P3; full TypeScript coverage and production reduction are still required.

The active 17-building registry is checked `data/buildings.ts` with unchanged exported-value hash, typed resource IDs, and compile-time building-reference checks. The grader's inherited P2 unknown-building save defect and P3 weak Estate tests are corrected: save validation rejects unknown object/string building types without changing original bytes, and the tests assert the exact Strip Farm cost, plot change, and refund. Both independent reviewers accepted the exact corrections at `A71E0D54…`, including 21/21 and 20/20 focused browsers, unit and typecheck passes, and no new scoped P0–P3. The last full 105-case gameplay browser run passed 104 cases, including six genuine campaigns, and timed out once waiting for a Load button on a blank title page in a Market test; the same test passed in isolation and in the next adjacent run. This first-attempt failure remains recorded.

The seasonal simulator is now `engine/economyEngine.ts`. An archived module and 180-case parity harness confirm identical numeric/report results and random draw counts across difficulty, seed, season, famine, luxury stock, legacy buildings, and damaged estates; 18 build cases and helper boundaries also match. The grader independently matched 1,200 old/new simulations and 16,800 helper outputs. Typechecking exposed missing purchased-provision quantities in the save contract; save validation now rejects invalid present salt/tools/spices and unknown tax rates. At `63480C77…`, both reviewers found a P2: extra saved inventory keys altered storage and production. The validator now rejects unrecognized keys against the authored resource registry, and `Inventory` lives in `data/economy.ts`. Both reviewers accepted their exact 390/1366 browser/direct retests at `9BE56B6E…` with no new scoped P0–P3. Local typecheck/lint/build, 89/89 units, 57/57 adjacent browsers, and 100 campaigns (55 victories, 45 losses, zero save failures) pass.

The seven strategy paths and 21 tiers are now authored in `data/synergies.ts`, with `engine/synergyEngine.ts` checking them. Both reviewers accepted conversion parity at `7FB3B66B…` but found inherited skipped faith/approval requirements, inert meter rewards, covered notifications, and incomplete saved tier sequences. Corrections are included in this checkpoint: live faith/approval conditions and counters, seasonal rewards, no duplicate counter increment after a perspective flip, tier-prefix save checks, and higher toast placement. The latest source-worktree typecheck and 95 unit tests passed. The corrections still need browser verification and independent retests; the overall slice remains IN PROGRESS. **When the user resumes development, finish those retests before continuing the engine/view migration and production-code reduction.** Most views and the main reducer remain JS/JSX. Current checkpoint checks are recorded in `docs/v2/checkpoint.md`.
